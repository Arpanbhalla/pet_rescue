
<ul>
  <% @users.each do |u| %>
    <li>
      <%= link_to( u.first_name, user_path( u ) ) %>
      <%#= link_to( u.name, user_path( u ) ) %>
      <%#= link_to( u.name, u ) %>
    </li>
  <% end %>
</ul>
