<h2 class ="heading">All animals for sale</h2>
<div class='animal_image'>
<% @animals.each do |w| %>
  <p>
    <%= link_to(image_tag(w.images, :class => "thumbnail"), animal_path(w)) %>
  </p>
<% end %>
</div>
